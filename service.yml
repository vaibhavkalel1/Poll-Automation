apiVersion: v1
kind: Service
metadata:
  name: poll-app-service1
spec:
  type: NodePort
  selector:
    app: poll-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30018 # You can change this to any available port number above 30000
